#Comenzamos con funciones

#Definimos una función
#hay que tener en cuenta la identación para que no quede código fuera de la función

#mi_funcion() #No se puede llamar a la función antes de ser declarada

def mi_funcion():
    print('Esta es una función')

mi_funcion() #estamos llamando a la función
mi_funcion() #estamos llamando a la función #puedo llamarla todas las veces que sea necesaria
